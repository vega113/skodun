# PR 180 delivery plan

1. Integrate current main into the verified PR head, preserving request identity,
   cancellation, separate execution budgets, FIFO ordering, and schema migrations.
2. Fix review findings: hold machine capacity safely for live reviews, validate
   falsey config values, and report the same resolved cap used by admission.
3. Harden recovery: serialize lifecycle replacement, re-inspect under lock,
   descriptor-based private quarantine, safe absolute CLI paths, full expected
   schema verification, and restrictive replacement permissions. Reject every
   observed integrity failure; keep ordinary healthy opens free of full scans.
4. Add hermetic regression tests at shipped store/admission/doctor/stats doors.
   Run focused suites, full suite, and separate ResourceWarning sweep.
5. Review frozen diff, address all actionable GitHub threads, push to PR 180,
   verify current-head CI and mergeability, merge, fetch and smoke-test main.

Self-review: scope is the existing PR plus demonstrated safety/integration fixes.
No live store manipulation, gate/trust changes, adapter additions, or schema
version rewrites. Existing mutable Ticket is a lifecycle handle; avoid a broad
immutability refactor. No linked issue is currently recorded on the PR.

Review refinement: add transactional v21 `capacity_admissions.owner_start`
for proven PID-reuse detection, bounded release retries, and integrate the newly
landed host launcher so its explicit two-slot profile sets the machine cap.
Verify migration preserves existing rows and historical nullable ownership.
